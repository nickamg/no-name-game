{
    "name": "no-name-game",
    "version": "0.0.1",
    "description": "The most fantastic bombastic game of all time",
    "dependencies": {
        "express": "^4.18.1",
        "socket.io": "^4.5.1"
    },
    "scripts": {
        "start": "cd ./frontend && npm run build && cd .. && node index.js",
        "server": "node index.js",
        "build": "cd ./frontend && npm install && npm run build",
        "buildOriginal": "cd ./frontend && npm run build"
    }
}
